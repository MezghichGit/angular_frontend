import { AddProviderPipe } from './add-provider-pipe';

describe('AddProviderPipe', () => {
  it('create an instance', () => {
    const pipe = new AddProviderPipe();
    expect(pipe).toBeTruthy();
  });
});
